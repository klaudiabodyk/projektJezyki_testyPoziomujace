[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Environment variables
[build.environment]
  VITE_META_PIXEL_ID = "675847502083478"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
